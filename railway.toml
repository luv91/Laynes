[deploy]
# v16.0: Single command - unified temporal CSV includes all rates (legacy + 2024 review)
# No more chaining multiple import scripts!
startCommand = "python scripts/populate_tariff_tables.py --reset && gunicorn wsgi:app --bind 0.0.0.0:$PORT"
